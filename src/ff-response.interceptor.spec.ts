import { FFResponseInterceptor } from './ff-response.interceptor';

describe('FfResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new FFResponseInterceptor()).toBeDefined();
  });
});
